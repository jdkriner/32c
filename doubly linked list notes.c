/*

List only has one node if head == tail

sort the list by iterating thru for min value, and then set sortedhead = to the node
when you put the min node in the sortedhead list, break head and tail pointers of the node